apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: test-chatbot-evan-database
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: test-chatbot-evan-database
  name: test-chatbot-evan-database
spec:
  ports:
    - port: 
      protocol: TCP
      targetPort: 
  selector:
    app.kubernetes.io/instance: test-chatbot-evan-database
